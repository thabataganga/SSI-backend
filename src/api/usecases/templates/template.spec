describe('CRUD - Lideranças', () => {
    test('deve adicionar uma liderenca', () => {
        expect(true).toEqual(false);
    });
    
    test('deve editar uma liderenca', () => {
        expect(true).toEqual(false);
    });
    
    test('deve remover uma liderenca', () => {
        expect(true).toEqual(false);
    });
    
    test('deve obter todas as liderencas', () => {
        expect(true).toEqual(false);
    });
    
    test('deve obter as liderencas por regex via nome', () => {
        expect(true).toEqual(false);
    });
    
    test('deve obter as liderencas por id', () => {
        expect(true).toEqual(false);
    });
});